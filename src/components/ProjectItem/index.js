import React from 'react'
import './ProjectItem.css'

const ProjectItem = ({title, time, image, teamSize, detail, techStacks, demo, source}) => {
  return (
    <div className='project-item__container'>
      updating...
    </div>
  )
}

export default ProjectItem
